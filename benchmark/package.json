{
  "private": true,
  "name": "wasm-brotli-benchmark",
  "version": "0.0.0",
  "scripts": {
    "build": "rollup -c",
    "prestart": "npm run build",
    "start": "node ./dist/index.js",
    "test": "npm start",
    "benchmark": "npm start"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0-beta.35",
    "@babel/preset-env": "^7.0.0-beta.35",
    "@babel/preset-stage-0": "^7.0.0-beta.35",
    "rollup": "^0.52.2",
    "rollup-plugin-babel": "^4.0.0-beta.0"
  },
  "dependencies": {
    "benchmark": "^2.1.4",
    "iltorb": "^2.0.3",
    "wasm-brotli": ".."
  }
}
